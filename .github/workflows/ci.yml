name: Python CI

# 워크플로우가 언제 실행될지 정의합니다 (트리거)
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest  # 최신 우분투 환경에서 실행
  
    permissions:
      contents: write
      pages: write      # 추가됨
      id-token: write   # 추가됨

    steps:
    # 1. GitHub 레포지토리의 코드를 가상 환경으로 가져옵니다.
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. 파이썬 환경을 설정합니다. (현재 사용 중이신 3.10 버전)
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        cache: 'pip' # 패키지 설치 속도를 높이기 위해 pip 캐시 적용

    # 3. 프로젝트 실행에 필요한 패키지들을 설치합니다.
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # ==========================================
    # [여기를 추가하세요!] 4. 테스트용 가짜 설정 파일 복사
    # ==========================================
    - name: Create Mock Config
      run: cp config/config.yaml.example config/config.yaml
    - name: Create data directory
      run: mkdir -p data

    # 5. 테스트 코드 실행
    - name: Run tests with pytest
      run: |
        pytest

    # 6. 커버리지(테스트 통과율)를 측정하고 싶다면 아래 명령어를 사용합니다.
    - name: Run tests with coverage
      run: |
        # 1) 터미널 출력(term-missing)을 파일로 저장하고, 동시에 xml도 만듭니다.
        pytest --cov=. --cov-report=term-missing --cov-report=xml:coverage.xml --cov-report=html:htmlcov > coverage.txt
        # [중요] 실제로 파일이 생겼는지 확인하기 위해 목록을 출력합니다.
        echo "Check if htmlcov exists:"
        ls -d htmlcov/ || echo "htmlcov directory NOT FOUND"
        ls -la htmlcov/
        ls -l coverage.xml

    - name: Post Coverage to Summary
      if: always()
      run: |
        echo "## 📊 코드 커버리지 리포트" >> $GITHUB_STEP_SUMMARY
        echo "### 상세 현황" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`text" >> $GITHUB_STEP_SUMMARY
        # 필요한 표 부분만 골라내거나 전체를 넣습니다.
        cat coverage.txt >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

    # 7. HTML 리포트를 Artifact로 저장 (브라우저 확인용)
    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: htmlcov/ # 리포트가 생성된 폴더 경로

    # 8) 별도의 브랜치 없이 GitHub Pages 서버에 즉시 배포합니다.
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Pytest coverage comment
      if: github.ref == 'refs/heads/main'
      uses: MishaKav/pytest-coverage-comment@main
      with:
        pytest-xml-coverage-path: ./coverage.xml
        title: "📊 코드 커버리지 리포트"
        badge-title: "Coverage"
        hide-comment: true
        hide-report: true
        # 🚀 [추가] 변경된 파일이 없어도 무조건 뱃지를 업데이트합니다.
        report-only-changed-files: false 
        github-token: ${{ secrets.GITHUB_TOKEN }}